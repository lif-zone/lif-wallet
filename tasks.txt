Task list
=========

make wallet.jsx support multiple wallets
----------------------------------------
+ today it is just a single wallet. It should be "boxes" of wallets,
  and a box for "New wallet". So when starting it first time it only has
  "New wallet" box. clicking on it - adds a new wallet, where you select
  all the things you sellect in today's wallet.jsx implementation:
  Network (Bitcoin, Bitcoin testnet, Lif), the wallet key etc...
+ Once a wallet is added, in its box is the amount of unspent coins it has,
  and amount of BTC/LIF it has.
+ clicking on it - opens up the wallet's transactions, and allows you to
  delete the wallet, and to send and receive from this wallet.
+ in the global settings, you can set the electrumx server for the different
  networks (Bitcoin, Bitcoin testnet, Lif...)
+ cache (c) and cache store (cs) for instant statup and screen switching
+ Settings screen: move Developer tools to separate screen.
+ in dev tools screen: Add a Memopool delete button /reset_mempool
+ move BTC test network into "Developer tools"
+ in Network options, put LIF by default, and first, BTC second. via Radio
  button.
+ dont show in BTC wallets the lif kv options
+ fees are not working: click Send.
+ never use lif_kv outputs for regular Send tx. Need to do a kv transfer tx
  and then the change will get "separated" back to us. Dust>1 protects this.
+ create tx with kv_lif controlling outputs with 1 value (like runs)
  (inscribe/edit/transfer). Dust>1 protects from transering kv by mistake.
+ fees are not working: click Send: dont throw Error() on fees - give a
  funding specific error (on no funds, or not enough funds, telling how much
  is missing. Should already appear on the page itself the missing funds,
  not as a surprise.
+ add click to mine
+ bug in edit/transfer domain: tx not appearing immediatly.
+ bug in transfer domain: transfer to self, TX is found in mempool.
  click "Mine", and then URL gets 400 Bad request on tx.
+ fees and funding error improvement: give clear warning when trying to
  create transactions with insufficient funding. separate problems between
  funding and fees, and show the total available balance. (after removing
  dust)
+ add refresh button on main page, and on wallet page to force fetch again
+ unconfirmed tx first in wallet, not last
+ show in wallet tx list txs with lif_kv: that they have lif kv in them,
  both in the tx list, and in tx detail list
+ add Mine button in TX page when there is unconfirmed TXs for that wallet,
  and when in Devtools mode
+ add qr for receive address
+ add qr for send address
+ replace alert() windows, with JS.
+ add Mine button on LIF wallets, to collect funds.
  + add JSON RPC getblocktemplate
+ calculate how many HPS needed to get on avarage a share of sats.
+ fix UI for aruk.localhost:4000 error msg
+ open new users in github, and re-import the projects, but with different
  login names/emails
+ separate lif-kernel from lif-os: create a new tree, and re-create commits.
+ run lif_node.js on VPS
+ lif-kernel: support nice git: webapp URLs
  + fix hi.js demo_index to have nice domain names.
  + fix all "site" in wallets to have nice domain names
+ sync VPS lif_node with local lif_node (GETBLOCKS fail/timeout response)
+ add rpc.on(), and do rpc.emit('close')
+ load qrcode dynamically, only when needed
+ add $ value: 1LIF==1USD. BTC - get dynamic. a click on the value changes
  the display. in USD - show up to the cent $40.41 LIF, $1500.00 BTC
  anything under a cent, show as a cent.
+ instant P2P mining: add to wallet ability offer instant mining.
  use lif-kernel server do websocket connection between peers on same
  subject ('mine_instant'). Some register as mine_instant servers, and others
  request mine_instant servers, and get mining offers.
  + add websocket /.lif.rg
  + add websocket json rpc bridging, and topic locating
  + connect browser/wallet_db.js to /.lif.rg, finding topic 'mine_instant'
  + add to wallet a instant mining server. it publicizes to mine_instant.
  + add to wallet an instant mining page (client). it requests blocks heades
    to mine. once receiving it - it mines, and sends results, and waits to
    get resulting TX to his address. Must take no more than 10 seconds.
  + rpc no result/error
  + instant_pool_server update total_h to count hashs so far
  + add simple reward UI of estimations how much will be earned,
    and how much expected per day
    of mining, and how much expected per month of mining.
    move existing UI to 'advanced'
+ UI for 404 domain not found: to create wallet, and mine, and put content
  on wallet. Ask for email, and when purchasing domain, register it as
  'owner' and when arriving to that domain, put it as "this domain is owned
  by xxx@gmail.com. If you would like to purchase this domain - contact
  xxx@gmail.com.
  + backup wallet widget: if wallet was not backed up: display words,
    and click <I wrote it down>. Then the words turn to ***, and another box
    appears: write the words. below it <I forgot the words> (left).
    Below: <OK>. if clicking OK and words do not match - it tells you cannot
    continue until you backup your wallet words.
    And tell "If forgot" if you for
    if you click Register domain without correct words - it will
  + if wallet does not have sufficient funds:
    instant mining progress bar % widget
+ UI bugs: instant mine progress: need to make first 50% - 50% of speed, then
  slow down from there (25%
+ instant mine: when success: should fill up progress bar, and show how much
  $ you won.
+ instant mine bug: not stopping mining after winning.
+ create 'My first LIF wallet' when starting the app with no wallets
+ UI bugs: when creating new wallet - need to go into wallet after creation
+ In wallets with 0 TX, add "start mining to get some $ LIF for free"
+ add support for npm imports not package.json
+ lpm refresh: lif/local - always check.
+ lpm refresh: git - when no-cache request sent
- tun: add rpc.rconnect() - remote connection. Gives an object similar to
  and rpc(), but with remove connection.
- lpm refresh: locate github latest releases, and releases by tags
- lif-net
  - add an outgoing proxy from lif_node to lif server.
  - add lif-net incoming connections to lif-coin:8432
  - add lif-net outgoing connections to github
  - make lif/git source to bring most up-to-date versions

For release
-----------
- add LIF production network with production target (4G).
  move current network to test network.
- prepare genesis block inscriptions:
  - 42 words, The 42 of 18: The Meaning of Lif
    The Meaning of Rich Life: The 42 of 10 18: 6-7
    Hamavdil ben kodesh le hol. Separating the 7 from 6.
  - add marking in genesis block of bitcoin block hash.
  - 5 book names: beginning, names, calling, talking, things.
  - the GUIDE, the BIBLE, the TORAH. HHUMS HTORA MDRIH
  - creating basecoin TX: adding 'kv' bitcoin/block/2323' val
    {"hash": "112233..."}. info will be collected via electrum, and checked it
    does not yet appear in the KV DB. coinbase KVs will be stored in a
    separate prefix than rest of KVs.

Low priority:
-------------
- UI solution to update version: instead of
  Devtools->Application->Service Worker->Update->Unregister
- instant mine: funding payouts for multiple wins
- register 1842 coin with SLIP
- add support for websocket subscription on new transactions, and updating
  accordingly. including tx from mempool (for instant mining win feedback).
- instant mining: add request for reduced fees, by providing prev tx, and
  asking to update it.
- add mining control CPU 5% - 100%, in drop down.
- in CPU usage, it continues adding more mining threads - up to 4.
- add support for WebGPU and WASM, and in NodeJS for native SHA instructions
- show in wallet tx list ALL txs with lif_kv. today it shows only the last
  tx per value.
- add support for kv_lif with only key and no val (missing 'val'):
  this allows "passing" existing keys from inputs to different
  specific outputs in same transaction, without modifing values.
- add support for any str (not only utf8/json) in lif kv server.
- add ipc keep alive: every 100ms update a place in memory. On the waiting
  side, if for 1000ms no update - it starts giving error messages
- move the boot_worker web worker to the service worker.
- make esplora load from LIF:
  - fork it into a new tree
  - add to lif kernel support for answeing index.html for any URL, and
    supporting SPA, and 'virtual 404'.
  - support lif kernel JsRx and make esplora run on it
- add support for lif no-cors via out servers routing requests in return
  to mining or tx with money.
- tx broadcast that fails should show a message that it failed
- in wallet domain operations: change to default just DNS and site, and
  clicking advanced will allow full JSON edit, and full key edit
- add support for additional bitcoin test networks.

React rewrite
-------------
Screen routing (screen state + all those {screen=='x' && <Component />}) → React Router (react-router-dom). Each screen becomes a <Route>,         
navigation becomes useNavigate(). This alone would cut a lot.                                                                                      
                                                                                                                                                   
Forms (manual useState per field, useFormValid, fee/address fields) → React Hook Form. Replaces most form state with useForm(), register(),        
handleSubmit(). Works well with validation.                                                                                                        

Async data fetching (the useEffect + setLoading + setConnErr + setBalance pattern repeated in every screen) → TanStack Query                       
(@tanstack/react-query). Replaces all that with useQuery() and handles caching, loading, error states automatically.                               
                                                                                                                                                   
Inline styles (every element has style={{...}}) → Tailwind CSS or a small component library like Radix UI primitives. Even just extracting shared  
styles to a styles.js object would help.                                                                                                           
                                                                                                                                                   
Modal system (the custom Modal_context) → Radix UI Dialog is a drop-in that handles accessibility, focus trapping, etc. for free.                  
                                                                                                                                                   
The highest ROI in order:                                                                                                                          
1. React Router — biggest reduction, the screen state machine is ~100 lines of logic                                                               
2. TanStack Query — eliminates repetitive fetch boilerplate across many screens                                                                    
3. React Hook Form — cleans up the form screens                                                                                                    

coding conventions:
-------------------
- no spaces around arrow functions:
  GOOD: (a, b)=>code
  BAD: (a, b) => code
- no spaces around === and ==
  GOOD: if (a===null)
  BAD: if (a === null)
- use == for string and simple number comparison
  GOOD: if (a=='test')
  BAD: if (a == 'test')
  BAD: if (a === 'test')
- dont use Promise(). dont use xxx.then(). Use await
  GOOD: await do_long_work(arg)
  BAD: do_long_work(arg).then()
- no statements on same if () line:
  GOOD:
    if (x==1)
      return;
  BAD:
    if (x==1) return;
- no space after async arrow functions:
  GOOD: async()=>...
  BAD: async ()=>...
- always update prompt.txt at end of every task with the prompt and the
  summary ("Done. ...").
